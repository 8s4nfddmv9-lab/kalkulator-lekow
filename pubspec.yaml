name: kalkulator_lekow
description: Offline technical bidirectional infusion calculator for iOS, web and Android.
publish_to: none
version: 0.1.3-beta.2+17

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
