name: kalkulator_lekow
description: Offline, bidirectional infusion calculator prototype for iOS and Android.
publish_to: none
version: 0.1.0-dev.3+3

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
